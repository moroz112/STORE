import {
    LOGIN_USER_SUCCESS
} from 'actionTypes';
const initialState = {};
export default (state = initialState, {type, payload}) => {
    switch (type) {
        case LOGIN_USER_SUCCESS:
            return Object.assign({}, payload);
        default:
            return state
    }
}
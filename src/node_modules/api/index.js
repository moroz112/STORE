import request from 'superagent';
export const fetchGoods = async () => {
    const response = await fetch('http://localhost:3012/artists', {method: 'POST'});
    const goods = await response.json();
    console.log(goods);

    return goods;
};
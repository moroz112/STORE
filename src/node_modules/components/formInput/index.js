import React from 'react';

const FormInput = ({onChange}) => {
    const setInputValue = (element) => {
        onChange(element.target.value)
    };
    return (
        <input className='form-control' id='inputPassword2' onChange={setInputValue} />
    )
};

export default FormInput